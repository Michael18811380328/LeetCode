/**
 * @param {number} n
 * @param {number} k
 * @return {number[][]}
 */
// 回溯算法
var combine = function(n, k) {
    let resultArr = [];
    // 新建一个递增的数组，长度是n
    let remain = new Array();
    
    // 先把第一个放到数组中，然后把剩余的第二个放进去，直到临时数组的长度是K
    
    // 然后把临时数组放在结果数组中
    
    // 然后把最后一个去除掉，然后放第二个元素
    
    // 循环结束后，继续回溯
    
    // 直到循环了全部的数字
};